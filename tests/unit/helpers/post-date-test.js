import { postDate } from 'branch-social/helpers/post-date';
import { module, test } from 'qunit';

module('Unit | Helper | post date');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = postDate([42]);
  assert.ok(result);
});
